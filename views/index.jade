extends layout

block content
  #container
    #content
      h3 Now playing
      #current
        img.thumbnail
        .info
          marquee.title
          .description
          button.vote 이곡은 별로에요 ㅠㅠ
      h3 Add Music
      div
        input#playId(type="text", placeholder="가이드를 보고 비디오 아이디만 입력 해 주세요")
        input#addMusic(type="button", value="등록")
      img#guide(src='/images/guide.png')
    #playlist
      h3 Playlist
      ul
